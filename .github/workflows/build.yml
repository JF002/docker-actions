name: Build PineTime Firmware
on:
  push:
      branches: [master]

jobs:
    build:
      runs-on: ubuntu-latest
      container:
        image: 'docker/Dockerfile'
        steps:
          - name: Build
            with:
              entrypoint: /opt/build.sh
              args: all